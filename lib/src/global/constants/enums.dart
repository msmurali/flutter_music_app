enum Routes {
  homeRoute,
  preferencesRoute,
  songsRoute,
}

enum StorageKey {
  favourites,
  recents,
  queue,
  queueIndex,
  playback,
  theme,
  view,
  sortType,
  orderType,
}

enum AppTheme {
  light,
  dark,
  system,
}

enum PlaybackMode {
  order,
  shuffle,
  repeat,
}

enum View {
  list,
  grid,
}
