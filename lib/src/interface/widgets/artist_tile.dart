import 'package:flutter/material.dart';

class ArtistTile extends StatelessWidget {
  const ArtistTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
