enum Routes {
  homeRoute,
  preferencesRoute,
  songsRoute,
}

enum StorageKey {
  favourites,
  recents,
  queue,
  queueIndex,
  playback,
  theme,
  view,
  gridSize,
  sortType,
  orderType,
}

enum AppTheme {
  light,
  dark,
  system,
}

enum PlaybackMode {
  order,
  shuffle,
  repeat,
}

enum View {
  list,
  grid,
}

enum Option {
  select,
  playNext,
  addToFavourites,
  addToPlaylist,
  removeFromFavourites,
  info,
  addAllToFavourites,
  addAllToPlaylists,
}
