enum Routes {
  homeRoute,
  preferencesRoute,
  songsRoute,
}

enum PreferencesKey {
  favourites,
  recents,
  queue,
  queueIndex,
  playback,
  theme,
  view,
  sortType,
  orderType,
}

enum AppThemes {
  light,
  dark,
  system,
}

enum PlaybackMode {
  order,
  shuffle,
  repeat,
}

enum View {
  list,
  grid,
}
